interface:
  display_name: "Narrate"
  short_description: "Explain one topic visually"
  default_prompt: "Use $narrate to turn this topic into a concise visual brief."
policy:
  allow_implicit_invocation: true
