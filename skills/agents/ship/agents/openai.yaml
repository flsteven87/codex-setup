interface:
  display_name: "Ship"
  short_description: "Review, deliver, merge, and deploy changes"
  default_prompt: "Use $ship to take the completed change through its full delivery path, including merge and deployment when applicable."
policy:
  allow_implicit_invocation: true
