interface:
  display_name: "Ship"
  short_description: "Deliver reviewed commits through merge and deploy"
  default_prompt: "Use $ship after $implement to deliver the reviewed commit through push, merge, deployment, and verification."
policy:
  allow_implicit_invocation: false
