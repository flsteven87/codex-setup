interface:
  display_name: "Catch Up"
  short_description: "Rebuild context from repo evidence"
  default_prompt: "Use $catchup to inspect the current repo state and recommend the next step."
policy:
  allow_implicit_invocation: true
