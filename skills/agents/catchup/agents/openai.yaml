interface:
  display_name: "Catch Up"
  short_description: "Resume a handoff against current repo evidence"
  default_prompt: "Use $catchup with a handoff file to reconcile the previous session with current repository state and recommend the safest next action."
policy:
  allow_implicit_invocation: false
